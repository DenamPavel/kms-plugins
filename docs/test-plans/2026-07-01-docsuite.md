# Human Test Plan — docsuite Phases 1–5 (kms-docs plugin)

Covers the mode-parameterized documentation-pipeline (`user-guide` / `maintainer` / `agents-md`), the `writing-internals` scope rulebook, the shared `prose-voice-rules` fragment, the internals investigator/critic, the grounding-artifact schema, the `doc-agents-md` distiller, and the bundled `agents-md-filter.mjs` script.

Verification model: this plugin has no unit-test framework by design. Each acceptance criterion is verified by one of: node-smoke-test (the one bundled script), subagent RED-GREEN, dispatch-inspection, static-grep, or operational end-to-end through the two human gates. The one mechanically executable test (AC4.2, `agents-md-filter.mjs`) and the AC6.2 standalone-posture grep were executed during implementation and both PASS.

Scope note: AC2, AC5, AC7, AC6.1/6.3/6.4, and the bundled cross-plugin static-check script are Phase 6–8, out of scope for this plan.

## Prerequisites

- Node.js installed (`node --version`). The bundled filter and capture scripts run on plain Node with no build step.
- Claude Code installed. Capture the version for AC4.6: `claude --version`.
- The plugin at `plugins/kms-docs/` loaded so `/write-doc` and its agents dispatch.
- A scratch directory outside any target repo for grounding artifacts (`mktemp -d`).
- Re-run the mechanical test to confirm the environment before the operational runs:
  `node plugins/kms-docs/scripts/agents-md-filter.mjs --agents-md <draft> --manifest-values <values.json>` (exit 1 on flags, 0 clean).

Build the ad-hoc fixture used across the maintainer/agents-md runs: a minimal Node app with `src/ingest.js` (validates, writes events) and `src/reduce.js` (folds events into window-keyed rollups), a `package.json` with `build`/`test`/`start` scripts, one enforced invariant in `reduce.js` ("a rollup is never emitted before its window closes"), a planted secret `API_TOKEN = "sk-live-abc123"` in `config.js`, a planted internal hostname `db-internal.corp.local` in `config.js`, and hand-labeled must-reject generic lines (for AC4.3) staged in a notes file (e.g. "write clean, maintainable code").

## Phase 1: Mode dispatch and refusal (AC1.1, AC1.2, AC1.3, AC1.6)

| Step | Action | Expected |
|------|--------|----------|
| 1 | Run `/write-doc "Login page /path/to/fixture-repo"` (no mode token) | Resolves to `user-guide`; grounds with `doc-investigator`; no error. |
| 2 | Run `/write-doc "Login page /path/to/fixture-repo maintainer"` | Resolves to `maintainer`; grounds with `doc-internals-investigator`; presents the inverted-leak-list GATE 1, no capture plan. |
| 3 | Run `/write-doc "Login page /path/to/fixture-repo agents-md"` | Resolves to `agents-md`; runs full internals investigation, no coverage critic, routes to `doc-agents-md`. |
| 4 | Run `/write-doc "Login page /path/to/fixture-repo bogus-mode"` | Refused with a clear error naming `user-guide`, `maintainer`, `agents-md`. Pipeline does NOT run; does NOT silently treat as `user-guide`. |
| 5 | Run `/write-doc "Login page /path/to/maintainer-notes-repo"` where the repo path contains the word "maintainer" but is not the trailing token | Treated as target + path, defaults to `user-guide` (no mis-parse of the path as a mode). |

## Phase 2: User-guide behavior preservation (AC1.1, AC1.4, AC1.5)

| Step | Action | Expected |
|------|--------|----------|
| 1 | Complete a bare `user-guide` run against a ~20-line fixture module | Draft produced by `doc-writer` against `writing-documentation`; reviewers are `doc-fact-checker`, `doc-editor` (opus), `doc-coverage-critic`; revised by `doc-reviser`. Identical role set to v2.1.0. |
| 2 | Inspect the draft's reviewers for the tells checklist | Editor applies the same checklist as v2.1.0; plant a fragment-idiosyncratic tell (a "false equation": "That decision is the permission system") and confirm the editor flags it in the checklist's own terms. |
| 3 | Grep the plugin: `grep -rl "tells checklist" plugins/kms-docs/skills` | The tells table appears in exactly one file, `prose-voice-rules/SKILL.md`. `writing-documentation` and `writing-internals` reference it by name only. |

## Phase 3: Maintainer doc (AC3.1, AC3.2, AC3.3, AC3.4, AC3.5)

| Step | Action | Expected |
|------|--------|----------|
| 1 | Run `maintainer` mode against the fixture; open the emitted `grounding.json` | Parses as JSON, schema-valid (all 8 fields, correct types). `machineryMap` enumerates `event-ingester` and `rollup-reducer` with data-flow edges; `invariants` includes the window-close guarantee with a `sourcePaths` entry. |
| 2 | Inspect `grounding.json` `invertedLeakList` | Contains `sk-live-abc123` (kind: token) and `db-internal.corp.local` (kind: internal-hostname), each with a source path. Does NOT contain `ingest`, `reduce`, `events`, or `rollups`. |
| 3 | Write an ad-hoc draft documenting `ingest` but omitting `reduce` and its invariant; dispatch `doc-internals-critic` against it + the artifact | Flags the omitted `reduce` component and the window-close invariant, each naming the `machineryMap`/`invariants` entry and source path. A draft covering everything yields zero fabricated omissions. |
| 4 | Paste the fixture's real `sk-live-abc123` and `db-internal.corp.local` into a draft that also names a module + the `events` table; dispatch `doc-fact-checker` with the inverted leak model | Flags the secret and the hostname; does NOT flag the module name or table name. |
| 5 | Insert a reversed data-flow claim in the draft (reducer feeds ingester); re-run the fact-checker | Catches the reversed claim against source independently of the leak check. |
| 6 | Drive the full maintainer run through GATE 1 and GATE 2 (human) | Doc describes data flow, module boundaries, invariants, and ≥1 why-decision. Editor/reviser under `writing-internals` do NOT flag architecture/table names. Both gates require explicit human approval before anything is written. |

## Phase 4: AGENTS.md distillation (AC4.1, AC4.3, AC4.4, AC4.5, AC4.7)

| Step | Action | Expected |
|------|--------|----------|
| 1 | Run standalone `agents-md` mode against the fixture (no maintainer doc, no surveyor) | Performs a full internals investigation (schema-valid artifact with populated `facts`), holds GATE 1, then the single result gate. Not a shallow/template path. |
| 2 | At the result gate, inspect the generated `AGENTS.md` Commands/Testing | Build/test/run commands appear verbatim from `facts`, with source citations. |
| 3 | Search the generated `AGENTS.md` for the hand-labeled must-reject generic lines | Each must-reject line (e.g. "write clean, maintainable code") is ABSENT; non-obvious project-specific lines survive. |
| 4 | Inspect for human-judgment sections | Appear as `[TODO: …]` placeholders, not invented advice. |
| 5 | Search the generated `AGENTS.md` for `sk-live-abc123` and `db-internal.corp.local` | Neither appears anywhere, not in a distilled section, not in a boundary line. Sensitive boundaries phrased generically ("never commit credentials"). |
| 6 | Verify commit timing against the fixture repo | Nothing is written/committed to the fixture before the result gate passes. |

## Phase 5: CLAUDE.md bridge, verified against installed Claude Code (AC4.6)

| Step | Action | Expected |
|------|--------|----------|
| 1 | At the result gate, review the bridge offer | Offered as a yes/no choice, uses `@AGENTS.md` outside backticks. |
| 2 | Decline the bridge | Only `AGENTS.md` is written; no `CLAUDE.md` created. |
| 3 | Accept the bridge in a fixture that already has a `CLAUDE.md` | The `@AGENTS.md` import is prepended, existing content preserved (not overwritten). |
| 4 | Live check: in a scratch repo, create `CLAUDE.md` containing `@AGENTS.md` and an `AGENTS.md` with a sentinel line; run Claude Code | The installed Claude Code resolves the import (sentinel reaches context). If installed behavior differs from the 2.1.197 finding, the emitted syntax must be corrected. |

## End-to-End: Standalone posture (AC6.2 slice)

Purpose: confirm no new piece invokes an external plugin/skill/general-purpose agent.

Steps: from `plugins/kms-docs/`, run `grep -nE "ed3d|kms-human-voice|general-purpose"` over each phase's new/edited files. Expect zero matches except the one benign declaration line in `doc-internals-investigator.md:79` (the standalone-posture prohibition text itself, not an invocation). Confirm all cross-file references in new pieces are in-plugin (`prose-voice-rules`, `writing-internals`, `grounding-artifact-schema.md`, the bundled filter script).

## Human Verification Required

| Criterion | Why Manual | Steps |
|-----------|------------|-------|
| AC3.3 | GATE 1 (scope) and GATE 2 (result) are human gates by nature | Drive the full maintainer run; at each gate confirm the presented scope/result and that editor+reviser applied machinery scope, not user-guide scope. |
| AC4.1, AC4.3, AC4.4, AC4.7 | Backed by the single result gate; AC4.3 is a judgment oracle | At the `agents-md` result gate, verify verbatim commands, absent generic filler, `[TODO]` placeholders, and no leaked secret/hostname before approving. |
| AC4.5 | GATE 1 + result gate | Confirm the standalone path is full-depth (not shallow), holds GATE 1, and gates the result. |
| AC1.5 | RED-GREEN must run against the installed Claude Code, not a mock | Plant a fragment-idiosyncratic tell and confirm the real opus `doc-editor` flags it in the checklist's terms. |
| AC4.6 | Verified against the tool's actual import behavior | Run the scratch-repo sentinel check with installed Claude Code. |

## Traceability

| Acceptance Criterion | Automated verification | Manual step |
|----------------------|------------------------|-------------|
| AC1.1 | dispatch-inspection of SKILL.md `user-guide` row | Phase 1 step 1; Phase 2 step 1 |
| AC1.2 | dispatch-inspection of `maintainer` row | Phase 1 step 2; Phase 3 step 6 (e2e) |
| AC1.3 | RED-GREEN on command + SKILL validation | Phase 1 steps 4–5 |
| AC1.4 | static-grep (single-source fragment) | Phase 2 step 3 |
| AC1.5 | RED-GREEN vs installed Claude Code | Phase 2 step 2 |
| AC1.6 | dispatch-inspection of `agents-md` row | Phase 1 step 3; Phase 4 step 1 (e2e) |
| AC3.1 | schema-valid `grounding.json` check | Phase 3 step 1; Phase 3 step 6 (doc half) |
| AC3.2 | RED-GREEN omitted-subsystem | Phase 3 step 3 |
| AC3.3 | RED-GREEN editor/reviser scope | Phase 3 step 6 (GATE 1 + GATE 2) |
| AC3.4 | RED-GREEN fact-checker leak/claim | Phase 3 steps 4–5 |
| AC3.5 | RED-GREEN investigator producer | Phase 3 step 2 |
| AC4.1 | operational | Phase 4 step 2 (result gate) |
| AC4.2 | node-smoke-test (executed, PASS) | Prerequisites re-run |
| AC4.3 | judgment oracle + result gate | Phase 4 step 3 |
| AC4.4 | operational | Phase 4 steps 4, 6 |
| AC4.5 | operational | Phase 4 step 1 |
| AC4.6 | live check vs installed Claude Code | Phase 5 steps 1–4 |
| AC4.7 | operational | Phase 4 step 5 |
| AC6.2 (slice) | static-grep (executed, PASS) | End-to-End: Standalone posture |

## Notes for Phase 6–8

- The AC6.2 grep has a benign false positive on the standalone-posture declaration line `doc-internals-investigator.md:79` ("You invoke no external plugin… No `ed3d-*`, `kms-*`"). For the Phase 8 bundled cross-plugin static-check script to run clean mechanically, exclude the prohibition-declaration line or phrase the constraint without literal `ed3d-`/`kms-` tokens.
- `writing-documentation/SKILL.md:8` still points to the external `kms-human-voice` skill ("pair it with"). It is pre-existing prose guidance (not an invocation, does not violate AC6.2), but now coexists with the bundled `prose-voice-rules` fragment. Decide during Phase 6+ housekeeping whether the bundled fragment fully supersedes that pointer.
